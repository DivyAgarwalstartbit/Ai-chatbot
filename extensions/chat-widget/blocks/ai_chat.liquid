<div id="ai-support-widget"></div>

<script>
window.AI_WIDGET = {
  shop: "{{ shop.permanent_domain }}",
  app_url: "https://sb-aic.onrender.com/",
  currency: "{{ shop.currency }}",
  assets: {
    no_product: "{{ 'no-product.jpeg' | asset_url }}"
  },
  customer: {
    id: "{{ customer.id }}",
    email: "{{ customer.email }}",
    first_name: "{{ customer.first_name }}"
  }
}
</script>

{{ 'chat-widget.css' | asset_url | stylesheet_tag }}
<script src="{{ 'chat-widget.js' | asset_url }}?v={{ 'now' | date: '%s' }}" defer></script>

{% schema %}
{
  "name": "AI Chat Assistant",
  "target": "body",
  "settings": []
}
{% endschema %}
